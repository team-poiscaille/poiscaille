/** Class representing a killed listener. */
class OnKilledListener {
  /**
   * @param {Function} onKilled
   */
  constructor(onKilled) {
    this.onKilled = onKilled;
  }
}

module.exports = OnKilledListener;
